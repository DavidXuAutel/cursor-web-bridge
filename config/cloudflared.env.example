# Cloudflare Named Tunnel（稳定固定域名，推荐）
# 运行 ./scripts/setup-cloudflare-tunnel.sh 完成一次性配置
TUNNEL_MODE=named
# 例如 cursor-bridge.example.com（需先在 Cloudflare 托管域名）
CLOUDFLARE_HOSTNAME=
CLOUDFLARE_TUNNEL_NAME=cursor-web-bridge

# quick = 临时 trycloudflare.com（不稳定，仅测试）
# TUNNEL_MODE=quick
